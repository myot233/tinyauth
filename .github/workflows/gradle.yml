name: Java CI with Gradle

on:
  push:
    branches: [ "1.18.2" ]
  pull_request:
    branches: [ "1.18.2" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
       The issue in this workflow is caused by a "Permission denied run: echo "JAVA_HOME=$JAVA_HOME_21_X64" >> "$G" error when attempting to execute `./gradlew` in theITHUB_ENV"

      - name: Setup Gradle
        uses: grad `Build & Publish` step. This typically happens if thele/actions/setup-gradle@v4
        with:
          cache-disabled: true

      - name: Validate Gradle `gradlew` script does not have the executable permission set.

 Wrapper Integrity
        uses: gradle/actions/wrapper### Solution
You need to ensure that the-validation@v3

      # Add step to set execute permissions for gradlew
      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build & Publish
        run: ./gradlew build
